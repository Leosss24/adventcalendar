import { NextRequest,NextResponse } from "next/server";import { env } from "cloudflare:workers";import { requireUser } from "@/lib/server-auth";
export async function GET(_:NextRequest,{params}:{params:Promise<{key:string}>}){try{await requireUser();const {key}=await params;const decoded=decodeURIComponent(key);if(!/^advent-[a-f0-9-]+\.(jpg|png|webp|gif)$/.test(decoded))return new NextResponse("No encontrado",{status:404});const object=await env.BUCKET.get(decoded);if(!object)return new NextResponse("No encontrado",{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set("etag",object.httpEtag);headers.set("cache-control","private, max-age=86400");return new NextResponse(object.body,{headers})}catch{return new NextResponse("No autorizado",{status:401})}}

